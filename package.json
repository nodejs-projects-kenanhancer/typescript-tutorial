{
  "name": "typescript-tutorial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd dist/src && node index",
    "dev": "npm run build && npm start",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.18",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
